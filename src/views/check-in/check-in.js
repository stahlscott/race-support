import React from 'react';
import { Container } from 'semantic-ui-react';

function CheckIn() {
  return (
    <Container>
      You're checkin in now baby
      {
        // typeahead select rider OR enter new
        // rider edit/entry has name, email, usac, bib, race select typeahead
      }
    </Container>
  );
}

export default CheckIn;
